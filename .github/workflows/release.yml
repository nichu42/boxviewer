name: Build and Release APK & AAB

on:
  release:
    types: [published, prereleased]
  push:
    tags:
      - 'v*.*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v7

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Decode release keystore
        env:
          KEYSTORE_B64: ${{ secrets.KEYSTORE_B64 }}
        run: echo "$KEYSTORE_B64" | base64 -d > my-upload-key.jks

      - name: Build release APK and AAB
        env:
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          chmod +x ./gradlew
          ./gradlew assembleRelease bundleRelease --no-daemon -Dorg.gradle.jvmargs="-Xmx2048m" -Dorg.gradle.workers.max=2 --warning-mode all

      - name: Extract version name
        id: version
        run: |
          version=$(sed -n 's/.*versionName = "\([^"]*\)".*/\1/p' app/build.gradle.kts)
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Rename release APK
        run: cp app/build/outputs/apk/release/app-release.apk "BoxViewer-${{ steps.version.outputs.version }}.apk"

      - name: Upload APK artifact for manual runs
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: BoxViewer-${{ steps.version.outputs.version }}
          path: BoxViewer-${{ steps.version.outputs.version }}.apk

      - name: Upload release APK to GitHub Releases
        if: github.event_name == 'release' || startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          files: BoxViewer-${{ steps.version.outputs.version }}.apk

      - name: Upload AAB to Google Play Open Testing
        if: (github.event_name == 'release' || startsWith(github.ref, 'refs/tags/v')) && secrets.GCP_SERVICE_ACCOUNT_KEY != ''
        uses: r2da/google-play-publisher@v1
        with:
          service-account-json-raw: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}
          package-name: de.nichu42.boxviewer
          aab-file: app/build/outputs/bundle/release/app-release.aab
          track: beta
          status: completed
